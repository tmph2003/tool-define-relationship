# =============================================================================
# Backend — Service
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: relationship-designer
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: relationship-designer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
